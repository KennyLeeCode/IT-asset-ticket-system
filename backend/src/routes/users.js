const express = require("express");
const router = express.Router();

// User management routes will be added in Part 3 and 4
// Placeholder so the server can start without errors

module.exports = router;
